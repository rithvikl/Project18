<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/LightBlue</item>
        <item name="android:colorBackground">@color/DarkBlue</item>
        <item name="colorPrimaryDark">@color/DarkerBlue</item>
        <item name="colorAccent">@color/Red</item>
        <item name="android:textColorPrimary">@color/Orange</item>
        <item name="actionBarStyle">@style/bar_style</item>
    </style>

    <style name="bar_style" parent="@style/Widget.AppCompat.ActionBar.Solid">
        <item name="titleTextStyle">@style/bar_style_title</item>
    </style>

    <style name="bar_style_title" parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/DarkBlue</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="TextView">
        <item name="android:padding">16dp</item>
        <item name="android:textColor">@color/Orange</item>
        <item name="android:textSize">20sp</item>
        <item name="android:maxLines">100</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">match_parent</item>
    </style>

    <style name="Button">
        <item name="android:background">@drawable/custom_solid_button</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textAlignment">center</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:textColor">@color/button_change</item>
        <item name="android:layout_marginTop">16dp</item>
    </style>

</resources>
